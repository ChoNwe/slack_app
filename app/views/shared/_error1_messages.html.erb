<% if @mworkspace.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@mworkspace.errors.count, "error") %>.
    </div>
    <ul>
    <% @mworkspace.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>